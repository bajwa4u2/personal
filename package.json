{
  "name": "ms-bajwa-public-web-foundation",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node verify.mjs",
    "deploy": "npm run build && npm run verify && wrangler deploy && npm run notify:indexnow",
    "notify:indexnow": "node notify-indexnow.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "wrangler": "^4.127.1"
  }
}
